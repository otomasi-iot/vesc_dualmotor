// Minimal ChibiOS type compatibility for STM32 HAL + CMSIS-RTOS2 build.
// This project is being migrated away from ChibiOS; keep this header small.
#ifndef CHTYPES_H_
#define CHTYPES_H_

#include <stdint.h>
#include <stdbool.h>

typedef int32_t msg_t;

#endif

